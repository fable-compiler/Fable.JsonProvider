{
  "private": true,
  "scripts": {
    "build": "fable build.fsx --run",
    "publish": "fable build.fsx --run publish"
  },
  "dependencies": {
    "@babel/core": "^7.3.3",
    "fable-compiler-js": "^1.0.0-beta-002",
    "fable-publish-utils": "^1.0.5"
  }
}
