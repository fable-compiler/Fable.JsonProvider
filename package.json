{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "dotnet build src && dotnet fable watch test -s -o dist --run vite",
    "publish": "dotnet fsi build.fsx publish"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^3.2.7",
    "@vitejs/plugin-react": "^2.1.0"
  }
}